import React from 'react';
import ReactDOM from 'react-dom';
import './tailwind.css'; // Assuming you have Tailwind CSS set up
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();

